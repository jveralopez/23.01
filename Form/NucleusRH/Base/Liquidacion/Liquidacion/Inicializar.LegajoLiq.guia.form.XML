<form:Form doc="SOURCE" extends="Nomad.simple.guia.form" name="NucleusRH.Base.Liquidacion.Liquidacion.LIQUIDACION.guia" xmlns:form="nomad_engine.form">
	<form:Title>
		<form:RowAligner name="title">
			<form:Row name="row1">
				<form:Label style="S3 LCol_w" label="Seleccion de Personal de Liquidacion" name="GEN"/>
			</form:Row>
		</form:RowAligner>
	</form:Title>
	<form:Center/>
	<form:Scripts>
		function OnLoad()
{    
	//Ctrl("ACCEPT").SetEnabled(Ctrl("GRID"). .shownRegs.length==0?false:true);
}

function FilterGrid()
{    
	//Ctrl("GRID").filter("",Ctrl("buscar").GetCValue());
	//Ctrl("ACCEPT").SetEnabled(Ctrl("GRID").shownRegs.length==0?false:true);
}                    

function ACCEPT()
{
	if (Ctrl("GRID").GetXValue()!="") Ctrl("main").DoEvent("ACCEPT");
}
	</form:Scripts>
</form:Form>
