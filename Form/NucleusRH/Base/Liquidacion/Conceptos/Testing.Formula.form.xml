<form:Form name="testing" xmlns:form="nomad_engine.form" doc="SOURCE">
	<form:Title>
		<form:Label name="lblTitulo" label="Formula del Concepto" style="S3 LCol_w"/>
	</form:Title>
	<form:Center width="100%">
		<form:TextBox required="0" enabled="NEW|EDIT" ysize="30" xsize="100" length="4000" label="" nvalue="#SOURCE:@formula" name="formula"/>
	</form:Center>
	<form:Bottom>
		<form:RowAligner name="botones" columns="1">
			<form:Row name="filaBotones">
				<form:Button visible="" note="Vuelve a la pantalla anterior del formulario" descrip="Volver a la pantalla anterior" img="back" label="Volver" name="BACK">
					<form:Event action="BACK" name="onclick" runat="server"/>
				</form:Button>
			</form:Row>
		</form:RowAligner>
	</form:Bottom>
</form:Form>

