<form:Form xmlns:form="nomad_engine.form" extends="NucleusRH.Base.Personal.Legajo.filter.form" doc="source" name="NucleusRH.Base.Personal.Interfaces.ExpImagenes.form" default_mode="EDIT">
	<form:Title>
		<form:Label label="Exportacion de imagenes del personal" style="S3 LCol_w" nowrap="1" name="lblTitulo"/>
		<form:Label visible="0" style="S3 LCol_w" label="Selecci&#243;n de Personas" name="lbl_titulo"/>
	</form:Title>
	<form:Bottom>
		<form:RowAligner name="rowaligner1">
			<form:Row name="row1">
				<form:Button visible="0" name="AGREGAR" descrip="Agregar una persona" label="Agregar" img="add" note="Volver al Menu princial">
					<form:Event action="ADD" runat="server" name="onclick"/>
				</form:Button>
			</form:Row>
		</form:RowAligner>
	</form:Bottom>
	<form:Scripts>
		function SendToServer()
{       
	GetProxy().StoreService().SetUserStore("PER_IDsExportar", array2stringcustom(arrB, "ROWS", "ROW"));
	return true;
}        

function Filtrar()
{               
 	if (Ctrl("list2").TotalRowsCount()==0)
 	{             
 		alert("No hay personas filtradas.");
 		return false;
 	}
 	
 	//if (Ctrl("list2").TotalRowsCount()&gt;100)
 	//{             
 	//	alert("Hay demasiados registros seleccionados, afine la b&#250;squeda.");
 	//	return false;
 	//}
	SendToServer();
	Ctrl("main").DoEvent("FILTER");
}

	</form:Scripts>
</form:Form>
