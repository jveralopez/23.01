===== foreach( [colecion] ) { } =====

La estructura de control **foreach** permite ejecutar un bloque de instrucciones por cada elemento contenido en la **coleción** estableciendo el cursor,
en cada elemento de la misma.